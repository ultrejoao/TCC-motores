# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# Banco
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# Configuracao
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# Seguranca
argon2-cffi==23.1.0
pyjwt==2.10.1
slowapi==0.1.9

# Cliente HTTP usado por scripts/seed_demo.py para passar pelo fluxo real da API
httpx==0.28.1

# ML (compartilhado com o pipeline em ml/)
#
# VERSOES FIXAS, e nao por preferencia de estilo: o artefato .joblib carrega
# arvores serializadas pelo scikit-learn. Abrir com uma versao diferente da que
# treinou emite InconsistentVersionWarning e, nas palavras da propria
# biblioteca, "pode levar a codigo quebrado ou resultados invalidos" — um
# diagnostico errado sem erro visivel.
#
# Estas sao as versoes que produziram model_kaist_full_v1 e model_field_single_v1.
# Ao retreinar com outras versoes, atualizar aqui junto.
numpy==2.4.2
scipy==1.17.1
pandas==3.0.1
scikit-learn==1.8.0
xgboost==3.4.1
joblib==1.5.3
pyarrow==25.0.1
